# The server files run on Linux — keep them LF whatever the checkout platform,
# or bash and systemd choke on the carriage returns.
* text=auto eol=lf
*.sh          text eol=lf
*.service     text eol=lf
*.yml         text eol=lf
*.png binary
*.jpg binary
